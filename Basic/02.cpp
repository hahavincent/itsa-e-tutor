#include <bits/stdc++.h>
using namespace std;

typedef long long lint;

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    int n;
    while(cin >> n) {
        cout << fixed << setprecision(1) << 1.6 * n << "\n";
    }
    return 0;
}

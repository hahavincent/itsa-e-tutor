#include <bits/stdc++.h>
using namespace std;

typedef long long lint;

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    double a;
    while(cin >> a) {        
        cout << fixed << setprecision(1) << a * 9 / 5 + 32 << endl;
    }
    return 0;
}
#include <bits/stdc++.h>
using namespace std;

typedef long long lint;

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    int a;
    while(cin >> a) {
        cout << a << " ";
        cout << a * a << " ";
        cout << a * a * a << endl;
    }
    return 0;
}